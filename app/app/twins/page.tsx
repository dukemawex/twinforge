import Link from "next/link"
import { getWorkspaceData } from "@/app/actions/data"
import { Button } from "@/components/ui/button"
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"
import { Plus,UserRound } from "lucide-react"
export default async function Twins(){const {twins}=await getWorkspaceData();return <div className="flex flex-col gap-8"><div className="flex items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">Identity models</p><h1 className="mt-2 text-3xl font-semibold">Digital twins</h1></div><Button render={<Link href="/app/twins/new"/>}><Plus data-icon="inline-start"/>New twin</Button></div>{twins.length===0?<Card><CardContent className="flex min-h-80 flex-col items-center justify-center gap-4 text-center"><UserRound className="text-muted-foreground"/><div><p className="font-medium">No digital twins</p><p className="mt-1 text-sm text-muted-foreground">Upload your own reference video and record consent to begin.</p></div><Button render={<Link href="/app/twins/new"/>}>Create your first twin</Button></CardContent></Card>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{twins.map(t=><Card key={t.id}><CardHeader><CardTitle>{t.name}</CardTitle><CardDescription className="font-mono uppercase">{t.status}</CardDescription></CardHeader><CardContent className="text-sm text-muted-foreground">Created {t.createdAt.toLocaleDateString()}</CardContent></Card>)}</div>}</div>}

import { Upload,Rows3 } from "lucide-react"
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Button } from "@/components/ui/button"
export default function Batch(){return <div className="flex flex-col gap-8"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">CSV production</p><h1 className="mt-2 text-3xl font-semibold">Batch studio</h1><p className="mt-2 text-muted-foreground">Upload a CSV with title, script, language, and tags. Rows are validated before any job is created.</p></div><Card><CardHeader><CardTitle>Import production sheet</CardTitle><CardDescription>No demo rows are preloaded.</CardDescription></CardHeader><CardContent className="flex min-h-72 flex-col items-center justify-center gap-4 text-center"><Rows3 className="text-muted-foreground"/><label className="flex cursor-pointer items-center gap-2 rounded-md border border-border px-4 py-2 text-sm"><Upload/>Choose CSV<Input type="file" accept=".csv,text/csv" className="sr-only"/></label><p className="text-xs text-muted-foreground">Required columns: title, script</p><Button disabled>Queue valid rows</Button></CardContent></Card></div>}

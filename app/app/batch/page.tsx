import { FileSpreadsheet, Rows3, Upload } from 'lucide-react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'

export default function Batch() {
  return <div className="flex flex-col gap-8"><header><p className="font-mono text-xs uppercase tracking-[.22em] text-primary">Batch production</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Scale your output</h1><p className="mt-3 max-w-2xl text-muted-foreground">Turn a structured content sheet into a coordinated production queue.</p></header><div className="grid gap-5 lg:grid-cols-[1fr_300px]"><Card><CardHeader><CardTitle>Import production sheet</CardTitle><CardDescription>Upload a CSV containing a title and script for each video.</CardDescription></CardHeader><CardContent><label className="flex min-h-80 cursor-pointer flex-col items-center justify-center gap-5 rounded-2xl border border-dashed bg-muted/20 p-8 text-center transition-colors hover:bg-muted/40"><span className="flex size-16 items-center justify-center rounded-2xl bg-primary/10"><Upload className="text-primary" /></span><div><p className="font-medium">Drop your CSV here</p><p className="mt-2 text-sm text-muted-foreground">or click to choose a file</p></div><span className="rounded-full border px-3 py-1 font-mono text-[10px] uppercase text-muted-foreground">CSV · title + script</span><Input type="file" accept=".csv,text/csv" className="sr-only" /></label><Button size="lg" className="mt-5 w-full" disabled><Rows3 data-icon="inline-start" />Review production rows</Button></CardContent></Card><Card className="h-fit"><CardHeader><CardTitle>Sheet format</CardTitle><CardDescription>Prepare one video per row.</CardDescription></CardHeader><CardContent className="flex flex-col gap-4"><div className="flex items-center gap-3 rounded-xl border p-3"><FileSpreadsheet className="text-primary" /><div><p className="text-sm font-medium">Required</p><p className="mt-1 font-mono text-xs text-muted-foreground">title, script</p></div></div><div className="rounded-xl border p-3"><p className="text-sm font-medium">Optional</p><p className="mt-1 font-mono text-xs leading-relaxed text-muted-foreground">language, tags</p></div></CardContent></Card></div></div>
}

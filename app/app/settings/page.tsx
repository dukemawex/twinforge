import { CheckCircle2, CircleDashed, Cpu, ShieldCheck } from 'lucide-react'
import { getGpuReadiness } from '@/app/actions/data'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
export default async function Settings(){const gpu=await getGpuReadiness();const checks=[['Service endpoint',gpu.endpoint],['Authentication token',gpu.token],['Webhook verification',gpu.webhookSecret]] as const;return <div className="flex max-w-4xl flex-col gap-8"><header><p className="font-mono text-xs uppercase tracking-[.22em] text-primary">Workspace</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Integration readiness</h1><p className="mt-3 text-muted-foreground">Review what is configured without exposing credentials in the browser.</p></header><Card><CardHeader><div className="flex items-start gap-4"><span className="flex size-11 items-center justify-center rounded-xl bg-primary/10"><Cpu className="text-primary"/></span><div><CardTitle>GPU production service</CardTitle><CardDescription className="mt-2">{gpu.connected?'Core connection details are configured.':'Your preparation flows are available while compute connection is pending.'}</CardDescription></div></div></CardHeader><CardContent className="flex flex-col gap-3">{checks.map(([label,ready])=><div key={label} className="flex items-center justify-between rounded-xl border p-4"><div className="flex items-center gap-3">{ready?<CheckCircle2 className="text-primary"/>:<CircleDashed className="text-muted-foreground"/>}<span className="text-sm font-medium">{label}</span></div><span className="font-mono text-xs uppercase text-muted-foreground">{ready?'Configured':'Pending'}</span></div>)}<div className="mt-2 flex items-start gap-3 rounded-xl bg-secondary p-4"><ShieldCheck className="text-secondary-foreground"/><p className="text-sm leading-relaxed text-secondary-foreground">Secrets are managed in project environment variables. Prepared jobs never enter processing until the service accepts them.</p></div></CardContent></Card></div>}

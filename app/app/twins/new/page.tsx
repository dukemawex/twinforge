import { TwinPreparationForm } from '@/components/preparation-forms'
export default function NewTwin(){return <div className="mx-auto flex max-w-5xl flex-col gap-8"><header><p className="font-mono text-xs uppercase tracking-[.22em] text-primary">Presenter setup</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Prepare a digital twin</h1><p className="mt-3 max-w-2xl text-muted-foreground">Secure the identity reference, optional voice sample, and explicit consent now. Training remains pending until compute is connected.</p></header><TwinPreparationForm/></div>}

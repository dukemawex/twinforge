import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Checkbox } from "@/components/ui/checkbox"
import { Alert,AlertDescription,AlertTitle } from "@/components/ui/alert"
import { Upload,ShieldCheck } from "lucide-react"
export default function NewTwin(){return <div className="mx-auto flex max-w-3xl flex-col gap-8"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">Twin setup / 01</p><h1 className="mt-2 text-3xl font-semibold">Create a digital twin</h1><p className="mt-2 text-muted-foreground">Only upload footage of yourself or a person who has given explicit permission.</p></div><Alert><ShieldCheck/><AlertTitle>Consent is required</AlertTitle><AlertDescription>TwinForge stores reference files privately and will not submit training until a GPU endpoint is configured.</AlertDescription></Alert><Card><CardHeader><CardTitle>Identity reference</CardTitle><CardDescription>Use a clear, well-lit 30–120 second video with a stable camera.</CardDescription></CardHeader><CardContent><form className="flex flex-col gap-6"><div className="flex flex-col gap-2"><Label htmlFor="name">Twin name</Label><Input id="name" placeholder="e.g. Product presenter"/></div><label className="flex min-h-48 cursor-pointer flex-col items-center justify-center gap-3 rounded-lg border border-dashed border-border bg-muted/30 text-center"><Upload className="text-primary"/><span className="font-medium">Choose your reference video</span><span className="text-xs text-muted-foreground">MP4 or MOV, up to 500 MB</span><Input type="file" accept="video/mp4,video/quicktime" className="sr-only"/></label><div className="flex items-start gap-3"><Checkbox id="consent"/><Label htmlFor="consent" className="leading-relaxed">I confirm I own this likeness or have explicit, documented consent to create and use this digital twin.</Label></div><Button disabled>Continue — GPU offline</Button></form></CardContent></Card></div>}

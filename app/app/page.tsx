import Link from "next/link"
import { getWorkspaceData } from "@/app/actions/data"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Cpu, Plus, Radio, Video } from "lucide-react"
export default async function Overview(){const d=await getWorkspaceData();return <div className="flex flex-col gap-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">Workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Production overview</h1><p className="mt-2 text-muted-foreground">Live records from your private workspace.</p></div><Button render={<Link href="/app/create"/>}><Plus data-icon="inline-start"/>Create video</Button></div><Alert><Radio/><AlertTitle>GPU endpoint is not configured</AlertTitle><AlertDescription>Rendering actions remain unavailable. Add your endpoint and webhook secret in Settings—TwinForge will never invent status or utilization data.</AlertDescription></Alert><div className="grid gap-4 md:grid-cols-3">{[{n:d.twins.length,t:"Digital twins",i:Cpu},{n:d.jobs.length,t:"Render jobs",i:Radio},{n:d.videos.length,t:"Videos",i:Video}].map(x=><Card key={x.t}><CardHeader className="flex-row items-center justify-between"><CardDescription>{x.t}</CardDescription><x.i className="text-muted-foreground"/></CardHeader><CardContent><CardTitle className="text-4xl font-mono">{x.n}</CardTitle></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Recent activity</CardTitle><CardDescription>Jobs appear here only after you submit them to a connected GPU server.</CardDescription></CardHeader><CardContent>{d.jobs.length===0?<div className="flex min-h-52 flex-col items-center justify-center gap-3 text-center"><Radio className="text-muted-foreground"/><p className="font-medium">No render jobs yet</p><p className="max-w-md text-sm text-muted-foreground">Connect compute in Settings, create a digital twin, then submit your first video.</p></div>:<div className="flex flex-col gap-3">{d.jobs.slice(0,5).map(j=><div key={j.id} className="flex items-center justify-between border-b border-border py-3"><span>{j.stage||j.kind}</span><span className="font-mono text-xs text-muted-foreground">{j.status}</span></div>)}</div>}</CardContent></Card></div>}

import { getTwins } from '@/app/actions/data'
import { VideoPreparationForm } from '@/components/preparation-forms'
export default async function Create({searchParams}:{searchParams:Promise<{prompt?:string;preset?:string}>}){const twins=await getTwins();const sp=await searchParams;return <div className="mx-auto flex max-w-5xl flex-col gap-8"><header><p className="font-mono text-xs uppercase tracking-[.22em] text-primary">Creator</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">{sp.preset==='lesson'?'Prepare a course lesson':'Prepare a video'}</h1><p className="mt-3 max-w-2xl text-muted-foreground">Complete the production brief and save it to the queue. Rendering begins only after GPU compute is connected.</p></header><VideoPreparationForm twins={twins} initialScript={sp.prompt||''} preset={sp.preset||''}/></div>}

import Link from 'next/link'
import { getWorkspaceData } from '@/app/actions/data'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { ArrowRight, MonitorPlay, Sparkles, UserRound } from 'lucide-react'

export default async function Create() {
  const { twins } = await getWorkspaceData()
  return <div className="flex flex-col gap-8"><header><p className="font-mono text-xs uppercase tracking-[.22em] text-primary">Creator</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Create a video</h1><p className="mt-3 text-muted-foreground">Choose a presenter, write the script, and shape the performance.</p></header><div className="grid gap-5 xl:grid-cols-[1fr_380px]"><Card><CardHeader><CardTitle>Script and direction</CardTitle><CardDescription>Write exactly what your presenter should say.</CardDescription></CardHeader><CardContent className="flex flex-col gap-6"><div className="flex flex-col gap-2"><div className="flex items-center justify-between"><Label htmlFor="script">Script</Label><span className="font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Studio editor</span></div><Textarea id="script" rows={15} placeholder="Write or paste your script here…" className="resize-none text-base leading-relaxed" /></div><div className="flex items-center gap-3 rounded-2xl border bg-muted/30 p-4"><Sparkles className="text-primary" /><div><p className="text-sm font-medium">Writing tools</p><p className="mt-1 text-xs text-muted-foreground">Script refinement and translation can be added to this workspace.</p></div></div><Button size="lg" disabled={twins.length === 0}>Generate video<ArrowRight data-icon="inline-end" /></Button></CardContent></Card><aside className="flex flex-col gap-5"><Card><CardHeader><CardTitle>Presenter</CardTitle><CardDescription>{twins.length ? `${twins.length} available in your studio` : 'Add a presenter to continue.'}</CardDescription></CardHeader><CardContent>{twins.length ? <div className="flex flex-col gap-2">{twins.map(twin => <button key={twin.id} className="flex items-center gap-3 rounded-xl border p-3 text-left hover:bg-muted"><span className="flex size-10 items-center justify-center rounded-lg bg-primary/10"><UserRound className="text-primary" /></span><span className="font-medium">{twin.name}</span></button>)}</div> : <Button variant="outline" className="w-full" render={<Link href="/app/twins/new" />}>Create digital twin<ArrowRight data-icon="inline-end" /></Button>}</CardContent></Card><Card><CardHeader><CardTitle>Preview</CardTitle></CardHeader><CardContent><div className="flex aspect-video items-center justify-center rounded-2xl border bg-muted/40"><MonitorPlay className="size-9 text-muted-foreground" /></div><p className="mt-4 text-xs leading-relaxed text-muted-foreground">Your composition will appear here before production.</p></CardContent></Card></aside></div></div>
}
